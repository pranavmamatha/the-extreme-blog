{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "@prisma/extension-accelerate": "^2.0.1",
    "@thisispranav/the-extreme-blog-common": "^1.0.0",
    "hono": "^4.7.11",
    "prisma": "^6.10.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
